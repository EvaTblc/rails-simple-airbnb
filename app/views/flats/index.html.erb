<div class="container mt-3">

  <div class="row">
    <div class="col-12">
      <h1 class="justify-content-center">All our flats (4)</h1>
    </div>
  </div>

  <div class="row">
  <% @flats.each do |flat| %>
      <div class="col-md-4 mb-4">
        <div class="card" style="width: auto;">
          <img src='https://images.unsplash.com/photo-1494526585095-c41746248156?w=1200' class="card-img-top" alt="...">
          <div class="card-body" style="background-color: #FAFAFA">
            <h5 class="card-title"><%= flat.name %></h5>
            <p class="card-text"><%= flat.address %></p>
            <i class="fa-solid fa-euro-sign"><%= flat.price_per_night %></i>
            <i class="fa-solid fa-people-roof"><%= flat.number_of_guests %></i>
          </div>
          <ul class="nav m-2 justify-content-around">
            <li class="nav-item">
              <%= link_to "More info", flat_path(flat), class:"link-primary" %>
            </li>
            <li class="nav-item">
              <%= link_to "Edit", edit_flat_path(flat), class: "link-info" %>
            </li>
            <li class="nav-item">
              <%= link_to "Remove", flats_path, data: {turbo_method: :delete, turbo_confirm: "Really sure ?"}, class:"link-danger" %>
            </li>
          </ul>
        </div>
    </div>
    <% end %>
  </div>
</div>
